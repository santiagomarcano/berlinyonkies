{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "@nomiclabs/hardhat-waffle": "^2.0.2",
    "autoprefixer": "^10.4.2",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.4",
    "hardhat": "^2.8.4",
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.23"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "ct": "npx hardhat compile && npx hardhat test",
    "cdl": "npx hardhat compile && npx hardhat run scripts/deploy.js --network localhost",
    "cdtn": "npx hardhat compile && npx hardhat run scripts/deploy.js --network mumbai",
    "cdtm": "npx hardhat compile && npx hardhat run scripts/deploy.js --network mainnet",
    "cdtbsc": "npx hardhat compile && npx hardhat run scripts/deploy.js --network bsctestnet",
    "deploy:rinkeby": "npx hardhat compile && npx hardhat run scripts/deploy.js --network rinkeby",
    "build-production": "npx hardhat compile && npx hardhat run scripts/deploy.js --network polygon",
    "node": "export NODE_OPTIONS=--openssl-legacy-provider && npx hardhat node"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "@pinata/sdk": "^1.1.23",
    "dotenv": "^16.0.0",
    "jotai": "^1.6.0",
    "next": "^12.1.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}